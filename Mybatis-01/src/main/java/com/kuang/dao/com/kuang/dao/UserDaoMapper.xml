<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.kuang.dao.com.kuang.dao.UserDao">
    <select id="getUserListUser" resultType="com.kuang.dao.com.kuang.pojo.User">
        select * from mybatis.user
    </select>
    <select id="getUserListUserById" parameterType="int" resultType="com.kuang.dao.com.kuang.pojo.User">
        select * from mybatis.user where id = #{id}
    </select>
    <select id="getUserListUserlike" parameterType="String" resultType="com.kuang.dao.com.kuang.pojo.User">
        select * from mybatis.user where name like "%"#{value}"%"
    </select>
    <insert id="insertUser" parameterType="com.kuang.dao.com.kuang.pojo.User">
        insert into mybatis.user (id, name, pwd ) values (#{id},#{name},#{pwd})
    </insert>
    <insert id="insertUser2" parameterType="map">
        insert into mybatis.user (id, name, pwd ) values (#{Userid},#{Username},#{Userpwd})
    </insert>
    <delete id="deleteUser" parameterType="int">
        delete from mybatis.user where id = #{id}
    </delete>
    <update id="updateUser" parameterType="com.kuang.dao.com.kuang.pojo.User">
        update mybatis.user set id = #{id}, name = #{name}, pwd = #{pwd} where id = #{id}
    </update>
</mapper>