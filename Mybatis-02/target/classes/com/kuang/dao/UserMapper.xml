<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.kuang.dao.UserMapper03">
    <select id="getUserListUser" resultType="com.kuang.pojo.User">
        select * from mybatis.user
    </select>
    <select id="getUserListUserById" parameterType="int" resultType="error">
        select * from mybatis.user where id = #{id}
    </select>
    <select id="getUserListUserlike" parameterType="String" resultType="otheruser">
        select * from mybatis.user where name like "%"#{value}"%"
    </select>
    <insert id="insertUser" parameterType="otheruser">
        insert into mybatis.user (id, name, pwd ) values (#{id},#{name},#{pwd})
    </insert>
    <insert id="insertUser2" parameterType="map">
        insert into mybatis.user (id, name, pwd ) values (#{Userid},#{Username},#{Userpwd})
    </insert>
    <delete id="deleteUser" parameterType="int">
        delete from mybatis.user where id = #{id}
    </delete>
    <update id="updateUser" parameterType="otheruser">
        update mybatis.user set id = #{id}, name = #{name}, pwd = #{pwd} where id = #{id}
    </update>
</mapper>