<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Config 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.kuang.dao.StudentMapper">
    <select id="getStudent2" resultMap="student2">
       select s.id, s.name sname, t.name tname, t.id tid from mybatis.student s, mybatis.teacher t where s.tid = t.id
    </select>
    <resultMap id="student2" type="com.kuang.pojo.Student">
        <result property="id" column="id"/>
        <result property="name" column="sname"/>
        <association property="teacher" javaType="com.kuang.pojo.Teacher">
            <result property="tid" column="tid"/>
            <result property="name" column="tname"/>
        </association>
    </resultMap>
    <select id="getTeacherName" resultType="com.kuang.pojo.TeacherName">
        SELECT name from  mybatis.teacher where id = #{id}
    </select>

    <select id="getStudent" resultMap="student1">
        select * from mybatis.student
    </select>
    <resultMap id="student1" type="com.kuang.pojo.Student">
        <result property="id" column="sid"/>
        <result property="name" column="name"/>
        <association property="teacher" column="tid" select="getTeacher">
        </association>
    </resultMap>
    <select id="getTeacher" resultType="com.kuang.pojo.Teacher">
        SELECT * from  mybatis.teacher where id = #{tid}
    </select>
</mapper>
