<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Config 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.kuang.dao.BolgMapper">
      <insert id="insertBlog" parameterType="com.kuang.pojo.Bolg" >
           insert into mybatis.blog (id, title, author, create_time, views)
             values (#{id}, #{title}, #{author}, #{createTime}, #{views})
      </insert>
    <select id="selectBlogIF" parameterType="map" resultType="com.kuang.pojo.Bolg">
        select * from mybatis.blog where 1=1
        <if test="author!=null">
            and author =#{author}
        </if>
        <if test="title!=null">
            and title = #{title}
        </if>
    </select>

    <select id="selectBlogIF2" parameterType="map" resultType="com.kuang.pojo.Bolg">
        select * from mybatis.blog
        <where>
            <if test="title!= null">
                and title = #{title}
            </if>
            <if test="author !=null">
                and author = #{author}
            </if>
        </where>

    </select>

    <select id="selectBlogIF3" parameterType="map" resultType="com.kuang.pojo.Bolg">
         select * from mybatis.blog
        <where>
            <choose>
                <when test="title!=null"> and title = #{title}</when>
                <when test="views!=null"> and views = #{views}</when>
                <otherwise> and author= #{author}</otherwise>
            </choose>
        </where>
    </select>

    <update id="updateBlogWithSet" parameterType="map">
        update mybatis.blog
        <set>
            <if test="author != null">author = #{author},</if>
            <if test="title != null">title = #{title}, </if>
        </set>
            where id = #{id}
    </update>

    <select id="selectBlogForeach" resultType="com.kuang.pojo.Bolg" parameterType="map">
        select * from mybatis.blog
        <where>
            <foreach collection="ids" item="id" open="and (" close=")" separator="or">
                id = #{id}
            </foreach>
        </where>
    </select>

</mapper>
